# Theme adapters

`index.json` is the registry for the versioned `infocard-theme-contract@1`. Existing `infocard-*-style` skills remain the human-facing compatibility packages; this registry supplies machine-readable capabilities and ownership boundaries. Adapter files do not duplicate authoring, source, visual-gate or publishing SOPs.
