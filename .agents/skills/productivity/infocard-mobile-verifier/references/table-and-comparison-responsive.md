# 表格与比较内容的移动表达

用于创建和验收 390px 窄屏下的参数表、矩阵和多对象比较内容。

- 桌面保留语义表；移动端优先提供由同一数据生成的带字段标签 card/list 表达。
- 必须保留每个比较维度和对象值，使用 `overflow-wrap:anywhere`，不得靠缩小整页或裁掉右侧字段通过。
- 确需保留宽表时，只让专用 `.table-wrap` 横向滚动，标题、卡片和页面本身不滚动。
- 机械检查页面宽度不超过 viewport；视觉检查字段可读、滚动可发现，且覆盖完整触发区域。

移动表达改变后，旧截图和旧结论失效，必须重新取证。
