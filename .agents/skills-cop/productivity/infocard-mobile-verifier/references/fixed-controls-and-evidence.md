# 固定控件与移动证据

`position: fixed` 本身不是缺陷；缺陷是控件覆盖正文、图表、图例、代码或页尾关键内容。

- 在真实窄屏检查首屏和页尾，必要时增加底部安全区或改回正常流。
- DOM 的 `scrollWidth`、HTTP、build 或单张截图都不能单独证明视觉通过。
- 视觉证据必须覆盖 Hero、主要网格、表格/代码、风险区和页尾；长页面需要覆盖中段。
- 截图或视觉分析不可用时使用 `VISUAL_PENDING`，不得升级为移动视觉通过。
